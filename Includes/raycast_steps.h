#ifndef RAYCAST_STEPS_H
# define RAYCAST_STEPS_H

# include "types.h"
# include "steps.h"

void	start_step(t_steps *steps, char *passed, double angle);
void	step(t_steps *steps, double angle);

#endif
